# Copy this file to .env for local development. NEVER commit real secrets.
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gpt-4o-mini
# Optional: enable the local test-mode (returns canned responses)
GEMINI_TEST_MODE=1
